name: demo
description: Demo application for vyuh_node_flow package

version: 1.0.0+1
publish_to: none

resolution: workspace
environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_mobx: ^2.3.0
  go_router: ^16.3.0
  http: ^1.2.0
  mobx: ^2.5.0
  vyuh_node_flow:
    path: ../vyuh_node_flow

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/data/
